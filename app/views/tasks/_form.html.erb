<h1>Do something</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>

  <%= f.submit %>
<% end %>

<p class="btn btn-default"><%= link_to "Go to the list", tasks_path(@tasks) %></p>
